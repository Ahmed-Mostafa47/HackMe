# نظام استعادة كلمة المرور - اختبار التدفق

## ✅ الخطوات المتبعة لإصلاح المشكلة:

### 1️⃣ تحديثات `App.jsx`:
- ✅ أضيفنا import للـ `ResetPasswordPage`
- ✅ أضيفنا handling للـ `/reset-password` route
- ✅ استخراج الـ token من URL parameters بـ `URLSearchParams`

### 2️⃣ تحديثات `ResetPasswordPage.jsx`:
- ✅ تحسين الـ redirect بعد النجاح

### 3️⃣ تحديثات `forgot_password.php`:
- ✅ إضافة تعليق توضيحي للرابط

---

## 🧪 خطوات الاختبار:

### الخطوة 1: تأكد من قاعدة البيانات
```sql
-- تنفيذ هذا الأمر للتأكد من وجود الجدول
SELECT * FROM password_resets LIMIT 1;
```

### الخطوة 2: اختبر النظام
1. افتح الصفحة الرئيسية: `http://localhost:5173`
2. انقر على "Forgot Password"
3. أدخل إيميل موجود في النظام
4. يجب أن تحصل على رسالة "بحث عن الإيميل"
5. افتح صندوق البريد (Gmail)
6. انقر على رابط "Reset Password" في الإيميل
7. يجب أن تُنقل إلى صفحة `/reset-password?token=xxxxx`
8. أدخل كلمة مرور جديدة
9. انقر "UPDATE_PASSWORD"
10. يجب أن تُعاد للـ Login

---

## 🔧 في حال عدم عمل الإيميل:

### المشكلة 1: الإيميل لم يصل
**الحل:**
- تحقق من bcc/spam folder في Gmail
- تأكد من تفعيل Gmail App Password (في `forgot_password.php`)
- استخدم Mailtrap.io للاختبار بدون Gmail

### المشكلة 2: الرابط يرجع للـ Login
**الحل:**
- تأكد من أن URL في الإيميل يحتوي على الـ token
- تفحص أن الـ token صحيح في قاعدة البيانات

### المشكلة 3: "Invalid or expired token"
**الحل:**
- تأكد من أن الـ token لم ينتهِ (ساعة واحدة من الإرسال)
- تحقق من أن الـ token لم يُستخدم مسبقاً

---

## 📱 تدفق العملية الآن:

```
1. User يدخل Forgot Password
   ↓
2. يدخل الإيميل ويضغط Submit
   ↓
3. Backend (forgot_password.php):
   - يتحقق من الإيميل
   - يولّد token عشوائي
   - يخزن hash(token) في DB
   - يرسل الإيميل مع الرابط
   ↓
4. User يفتح الإيميل وينقر الرابط
   ↓
5. React يستخرج token من URL
   ↓
6. ResetPasswordPage تعرض نموذج كلمة المرور
   ↓
7. User يدخل كلمة المرور الجديدة
   ↓
8. Frontend تطلب reset_password.php مع token و password
   ↓
9. Backend يتحقق من token ويحدث كلمة المرور
   ↓
10. Frontend تُعاد للـ Login ✅
```

---

## ✨ الآن يجب أن يعمل النظام بدون مشاكل!
